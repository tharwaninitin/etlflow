dbLog = {
  url = ${LOG_DB_URL},
  user = ${LOG_DB_USER},
  password = ${LOG_DB_PWD},
  driver = ${LOG_DB_DRIVER}
}
timezone = "Asia/Kolkata"
slack =  {
  url = "",
  env = "dev"
}
dataProc = {
  mainClass = "examples.LoadData"
  depLibs = []
}